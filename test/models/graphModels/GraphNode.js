require('dotenv').config();

const assert = require("assert");

let GraphNode


describe("Model Line Tests", function() {
  before(done => {
    done()
  });

  after(done => {
    done();
  });

  it('should return all stations')

  it('should return station by ID')

  it('should return station by name')

  it('should return null if not found')

  it('should raise error if params are not correct')
});
